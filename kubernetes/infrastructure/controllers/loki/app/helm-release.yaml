# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: loki
spec:
  interval: 10m
  chartRef:
    kind: OCIRepository
    name: loki
  # retries: -1 means retry indefinitely. Loki's PVCs bind to ceph-block
  # which is provisioned by rook-ceph-cluster from a sibling Kustomization
  # tree - Flux retries until the storage class exists instead of failing.
  install:
    remediation:
      retries: -1
  upgrade:
    remediation:
      retries: -1
  # The OBC `loki-bucket` uses generateBucketName, so the actual bucket name
  # is suffixed with a UUID. Flux valuesFrom pulls the generated BUCKET_NAME
  # out of the Rook-emitted ConfigMap and patches it into loki.storage.bucketNames.
  valuesFrom:
    - kind: ConfigMap
      name: loki-bucket
      valuesKey: BUCKET_NAME
      targetPath: loki.storage.bucketNames.chunks
    - kind: ConfigMap
      name: loki-bucket
      valuesKey: BUCKET_NAME
      targetPath: loki.storage.bucketNames.ruler
    - kind: ConfigMap
      name: loki-bucket
      valuesKey: BUCKET_NAME
      targetPath: loki.storage.bucketNames.admin
  # Durable log storage lives in the Ceph RGW S3 bucket (loki-chunks).
  # The backend/read/write PVCs here are index/cache only and are NOT
  # covered by Volsync by design - on rebuild, the index is rebuilt from
  # the chunks in S3. See loki storage.s3 config below for the bucket.
  values:
    global:
      dnsService: dns-default
      dnsNamespace: openshift-dns
    deploymentMode: SimpleScalable
    loki:
      auth_enabled: false
      storage:
        type: s3
        s3:
          endpoint: rook-ceph-rgw-okd-ceph-objectstore.rook-ceph.svc.cluster.local:80
          region: us-east-1
          insecure: true
          s3ForcePathStyle: true
        bucketNames:
          # Actual values are patched in from the loki-bucket ConfigMap via
          # spec.valuesFrom above; these placeholders are overwritten at render.
          chunks: loki-chunks
          ruler: loki-chunks
          admin: loki-chunks
      schemaConfig:
        configs:
          - from: "2024-04-01"
            store: tsdb
            object_store: s3
            schema: v13
            index:
              prefix: loki_index_
              period: 24h
      limits_config:
        retention_period: 336h
      compactor:
        retention_enabled: true
        delete_request_store: s3
    backend:
      replicas: 2
      extraEnvFrom:
        - secretRef:
            name: loki-bucket
        - configMapRef:
            name: loki-bucket
      persistence:
        storageClass: ceph-block
        size: 10Gi
    read:
      replicas: 2
      extraEnvFrom:
        - secretRef:
            name: loki-bucket
        - configMapRef:
            name: loki-bucket
      persistence:
        storageClass: ceph-block
        size: 10Gi
    write:
      replicas: 2
      extraEnvFrom:
        - secretRef:
            name: loki-bucket
        - configMapRef:
            name: loki-bucket
      persistence:
        storageClass: ceph-block
        size: 10Gi
    gateway:
      replicas: 1
    chunksCache:
      enabled: false
    resultsCache:
      enabled: false
    lokiCanary:
      enabled: false
    minio:
      enabled: false
    test:
      enabled: false
    monitoring:
      serviceMonitor:
        enabled: true
